<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only overlay: allow plain-HTTP LAN Jellyfin servers during development. Release
     builds require HTTPS so the api key never travels in cleartext. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:usesCleartextTraffic="true" />
</manifest>
